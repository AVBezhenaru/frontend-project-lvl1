name: Fitrst Project

on:
  - push

jobs:
  build:

    strategy:
      matrix:
        platform: [windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Run linter
        run: |
          make lint
  
   
